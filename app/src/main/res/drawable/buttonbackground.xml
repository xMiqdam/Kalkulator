<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#D4D4D4" /> <!-- Warna latar belakang -->
    <corners android:radius="5dp" /> <!-- Radius sudut (opsional) -->
    <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp" /><!-- Padding (opsional) -->
    <stroke
        android:color="#FFFFFF"
        android:width="2dp"
        />
</shape>